services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: clawgpt_agent
    ports:
      - "5432:5432"
    volumes:
      - clawgpt_pg:/var/lib/postgresql/data

volumes:
  clawgpt_pg:
